# Base URL of the FastAPI backend. This is a NEXT_PUBLIC_* variable, so it is
# baked into the browser bundle at build time — never put secrets here.
NEXT_PUBLIC_API_URL=https://api.deluxesalonsongs.com
